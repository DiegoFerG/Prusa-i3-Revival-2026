# 2026-09-13 teardown and inventory photo batch

Stage 01 is now **complete**.

## Original disassembly batch

This batch contains **119 original photographs** captured during and immediately after the printer teardown.

- IDs: `TD-20260913-041` through `TD-20260913-159`
- Destination: `photos/02-teardown/originals/`
- Images were preserved byte-for-byte from the supplied archive; only filenames were changed.
- `inventory-20260913.csv` maps each renamed file to its original camera filename.
- `SHA256SUMS-20260913.txt` provides integrity hashes for the original batch.

Together with the pre-teardown originals `TD-20260913-001` through `040`, the Stage 01 originals run continuously through `159`.

## Cleaned component-inventory derivatives

After disassembly, cleaned/measurement photographs were produced as privacy-sanitized derivatives:

- IDs: `TD-20260913-160` through `TD-20260913-228`
- Count: **69 derived JPEGs**
- Destination: `photos/02-teardown/derived/component-inventory/`
- Each image was decoded, oriented and re-encoded from pixels without preserving EXIF, GPS, XMP or device metadata.
- `derived/component-inventory/manifest.csv` is the authoritative derivative manifest.
- `derived/component-inventory/SHA256SUMS.txt` contains derivative integrity hashes.

## Stage total

The Stage 01 `TD-` sequence therefore contains **228 documented images**:

- 159 originals;
- 69 cleaned component-inventory derivatives.

The archive is not a frame-by-frame video of every screwdriver turn. Its purpose is to preserve enough original geometry, wiring, component state, measurements and cleaned inventory evidence to support reconstruction and future design decisions.

Some printed parts show visible damage. Damage timing must remain unclassified unless independently confirmed as pre-existing or caused during teardown.
